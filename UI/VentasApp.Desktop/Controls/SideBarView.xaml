<UserControl x:Class="VentasApp.Desktop.Views.Controls.SidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:VentasApp.Desktop.ViewModels.Base"
             mc:Ignorable="d">

    <Border Background="White" Width="250" BorderBrush="#EEEEEE" BorderThickness="0,0,1,0">
        <DockPanel>
            <!-- Título del negocio -->
            <StackPanel DockPanel.Dock="Top" Margin="20,25" Orientation="Horizontal">
                <Viewbox Width="24" Height="24" Margin="0,0,10,0">
                    <TextBlock Text="🏪" Foreground="#007BFF"/>
                </Viewbox>
                <TextBlock Text="{Binding NombreNegocio}" 
                           FontSize="20" 
                           FontWeight="SemiBold" 
                           VerticalAlignment="Center"
                           Foreground="#333333"/>
            </StackPanel>

            <!-- Items del Sidebar -->
            <ItemsControl ItemsSource="{Binding Items}" Margin="10,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:SidebarItemViewModel}">
                        <Button Command="{Binding Command}" Margin="0,2" Padding="10"
                                HorizontalContentAlignment="Left">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Icon}" Width="25"/>
                                <TextBlock Text="{Binding Title}" FontWeight="Medium"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DockPanel>
    </Border>
</UserControl>
