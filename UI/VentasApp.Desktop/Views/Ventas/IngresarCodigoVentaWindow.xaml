<Window x:Class="VentasApp.Desktop.Views.Ventas.IngresarCodigoVentaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Nueva Venta - Ingresar Codigo" 
        Height="280" 
        Width="450"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource SecondaryPastel}"
        ResizeMode="NoResize">
    <Grid>
        <Border CornerRadius="12" Background="{DynamicResource SurfaceCard}" Padding="24" Margin="16">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="12" Opacity="0.2"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                    <Border Width="40" Height="40" CornerRadius="20" Background="{DynamicResource SecondaryPastel}">
                        <materialDesign:PackIcon Kind="Barcode" Width="22" Height="22" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource TextDark}"/>
                    </Border>
                    <TextBlock Text="Codigo de Venta" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="{DynamicResource TextDark}"/>
                </StackPanel>

                <!-- Descripcion -->
                <TextBlock Grid.Row="1" Text="Ingrese el codigo para la nueva venta" FontSize="13" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,16" TextWrapping="Wrap"/>

                <!-- Campo de codigo -->
                <TextBox x:Name="TxtCodigo" Grid.Row="2"
                         materialDesign:HintAssist.Hint="Codigo de venta"
                         Style="{StaticResource CustomOutlinedTextBox}"
                         FontSize="16"
                         Margin="0,0,0,8"
                         KeyDown="OnCodigoKeyDown"/>

                <!-- Sugerencia del siguiente codigo -->
                <TextBlock Grid.Row="3" VerticalAlignment="Top" Margin="0,4,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextMuted}"/>
                            <Setter Property="FontStyle" Value="Italic"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=TxtSiguienteCodigo, Path=Text}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                    <Run Text="Sugerencia: "/>
                    <Run x:Name="TxtSiguienteCodigo" FontWeight="SemiBold"/>
                </TextBlock>

                <!-- Botones -->
                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="{DynamicResource AccentRed}"
                            BorderBrush="{DynamicResource AccentRed}"
                            Margin="0,0,8,0"
                            Click="OnCancelarClick">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Close" Width="16" Height="16" Foreground="{DynamicResource TextOnDark}"/>
                            <TextBlock Text="Cancelar" Margin="6,0,0,0" Foreground="{DynamicResource TextOnDark}"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="{DynamicResource AccentBlue}"
                            BorderBrush="{DynamicResource AccentBlue}"
                            Click="OnAceptarClick">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Check" Width="16" Height="16" Foreground="{DynamicResource TextOnDark}"/>
                            <TextBlock Text="Aceptar" Margin="6,0,0,0" Foreground="{DynamicResource TextOnDark}"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
