<UserControl x:Class="VentasApp.Desktop.Views.Cliente.ClienteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:VentasApp.Desktop.ViewModels.Cliente"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <!-- DataContext set via constructor injection in code-behind -->

    <Grid Background="{DynamicResource SecondaryPastel}" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Encabezado y boton agregar -->
        <DockPanel Grid.Row="0" LastChildFill="False" Margin="10">
            <TextBlock Text="Listado de Clientes" 
               FontSize="24" FontWeight="Bold" 
               Foreground="{DynamicResource TextDark}"
               DockPanel.Dock="Left" 
               VerticalAlignment="Center"/>

            <Button Style="{StaticResource MaterialDesignRaisedButton}"
            Command="{Binding AgregarClienteCommand}"
            Background="{DynamicResource AccentBlue}"
            BorderBrush="{DynamicResource AccentBlue}"
            DockPanel.Dock="Right"
            ToolTip="Agregar un nuevo cliente">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="AccountPlus" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="NUEVO CLIENTE"/>
                </StackPanel>
            </Button>

            <!-- Filtros -->
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" Margin="0,0,20,0" VerticalAlignment="Center">
                <TextBlock Text="Filtro:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource TextMuted}"/>
                <ComboBox ItemsSource="{Binding FiltrosDeuda}" 
                          SelectedItem="{Binding FiltroDeuda}" 
                          Width="120" 
                          Style="{StaticResource CustomFilledComboBox}"/>
            </StackPanel>
        </DockPanel>

        <!-- Listado -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="5">
            <ItemsControl ItemsSource="{Binding Clientes}"
                          ItemTemplate="{StaticResource ClienteCardTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
