<Window x:Class="VentasApp.Desktop.Views.Productos.EditarProductoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Editar Producto" Height="600" Width="520" WindowStartupLocation="CenterOwner">
    <Grid Background="{DynamicResource SecondaryPastel}">
        <Border CornerRadius="12" Background="{DynamicResource SurfaceCard}" Padding="16" Margin="16">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="12" Opacity="0.2"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,12">
                    <Border Width="40" Height="40" CornerRadius="20" Background="{DynamicResource SecondaryPastel}" DockPanel.Dock="Left">
                        <materialDesign:PackIcon Kind="Tag" Width="22" Height="22" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource TextDark}"/>
                    </Border>
                    <TextBlock Text="Editar producto" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource TextDark}"/>
                </DockPanel>

                <!-- Content -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="TxtNombre"
                             materialDesign:HintAssist.Hint="Nombre"
                             Style="{StaticResource CustomOutlinedTextBox}"
                             Margin="0,0,0,12"
                             MinWidth="320"/>

                    <!-- Talle (opcional) -->
                    <StackPanel x:Name="PanelTalle" Grid.Row="1" Margin="0,0,0,12">
                        <TextBlock Text="Talle (opcional)"
                                   FontSize="11" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,4"/>
                        <StackPanel>
                            <!-- Talles numéricos -->
                            <WrapPanel>
                                <RadioButton x:Name="TalleSinTalle" Content="—"   GroupName="TallesEditar" Margin="4,2" Style="{StaticResource MaterialDesignChoiceChipPrimaryOutlineRadioButton}" ToolTip="Sin talle"/>
							</WrapPanel>
							<WrapPanel>
                                <RadioButton x:Name="Talle4"        Content="4"   GroupName="TallesEditar" Margin="4,2" Style="{StaticResource MaterialDesignChoiceChipPrimaryOutlineRadioButton}"/>
                                <RadioButton x:Name="Talle6"        Content="6"   GroupName="TallesEditar" Margin="4,2" Style="{StaticResource MaterialDesignChoiceChipPrimaryOutlineRadioButton}"/>
                                <RadioButton x:Name="Talle8"        Content="8"   GroupName="TallesEditar" Margin="4,2" Style="{StaticResource MaterialDesignChoiceChipPrimaryOutlineRadioButton}"/>
                                <RadioButton x:Name="Talle10"       Content="10"  GroupName="TallesEditar" Margin="4,2" Style="{StaticResource MaterialDesignChoiceChipPrimaryOutlineRadioButton}"/>
                                <RadioButton x:Name="Talle12"       Content="12"  GroupName="TallesEditar" Margin="4,2" Style="{StaticResource MaterialDesignChoiceChipPrimaryOutlineRadioButton}"/>
                                <RadioButton x:Name="Talle14"       Content="14"  GroupName="TallesEditar" Margin="4,2" Style="{StaticResource MaterialDesignChoiceChipPrimaryOutlineRadioButton}"/>
                                <RadioButton x:Name="Talle16"       Content="16"  GroupName="TallesEditar" Margin="4,2" Style="{StaticResource MaterialDesignChoiceChipPrimaryOutlineRadioButton}"/>
                            </WrapPanel>
                            <!-- Talles con letra -->
                            <WrapPanel Margin="0,4,0,0">
                                <RadioButton x:Name="TalleS"        Content="S"   GroupName="TallesEditar" Margin="4,2" Style="{StaticResource MaterialDesignChoiceChipPrimaryOutlineRadioButton}"/>
                                <RadioButton x:Name="TalleM"        Content="M"   GroupName="TallesEditar" Margin="4,2" Style="{StaticResource MaterialDesignChoiceChipPrimaryOutlineRadioButton}"/>
                                <RadioButton x:Name="TalleL"        Content="L"   GroupName="TallesEditar" Margin="4,2" Style="{StaticResource MaterialDesignChoiceChipPrimaryOutlineRadioButton}"/>
                                <RadioButton x:Name="TalleXL"       Content="XL"  GroupName="TallesEditar" Margin="4,2" Style="{StaticResource MaterialDesignChoiceChipPrimaryOutlineRadioButton}"/>
                            </WrapPanel>
                        </StackPanel>
                    </StackPanel>

                    <TextBox x:Name="TxtCosto" Grid.Row="2"
                             materialDesign:HintAssist.Hint="Costo"
                             Style="{StaticResource CustomOutlinedTextBox}"
                             Margin="0,0,0,12"
                             MinWidth="320"/>

                    <TextBox x:Name="TxtPrecioVenta" Grid.Row="3"
                             materialDesign:HintAssist.Hint="Precio de venta"
                             Style="{StaticResource CustomOutlinedTextBox}"
                             Margin="0,0,0,0"
                             MinWidth="320"/>

                    <!-- Stock fields (optional) -->
                    <TextBox x:Name="TxtCantidadInicial" Grid.Row="4"
                             materialDesign:HintAssist.Hint="Cantidad inicial de stock (opcional)"
                             Style="{StaticResource CustomOutlinedTextBox}"
                             Margin="0,12,0,12"
                             MinWidth="320"/>
                   
                    
                    </Grid>
                </ScrollViewer>

                <!-- Actions -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="{DynamicResource AccentRed}"
                            BorderBrush="{DynamicResource AccentRed}"
                            Margin="0,0,10,0"
                            Click="OnCancelarClick">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Close" Width="16" Height="16" Foreground="{DynamicResource TextOnDark}"/>
                            <TextBlock Text="Cancelar" Margin="6,0,0,0" Foreground="{DynamicResource TextOnDark}"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="{DynamicResource AccentBlue}"
                            BorderBrush="{DynamicResource AccentBlue}"
                            Click="OnGuardarClick">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ContentSave" Width="16" Height="16" Foreground="{DynamicResource TextOnDark}"/>
                            <TextBlock Text="Guardar" Margin="6,0,0,0" Foreground="{DynamicResource TextOnDark}"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
