<UserControl x:Class="VentasApp.Desktop.Views.Productos.ProductoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:VentasApp.Desktop.ViewModels.Productos"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <!-- DataContext set via constructor injection in code-behind -->

    <Grid Background="{DynamicResource SecondaryPastel}" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" LastChildFill="False" Margin="10">
            <TextBlock Text="Listado de Productos" 
                       FontSize="24" FontWeight="Bold" 
                       Foreground="{DynamicResource TextDark}"
                       DockPanel.Dock="Left" 
                       VerticalAlignment="Center"/>

            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding AgregarProductoCommand}"
                    Background="{DynamicResource AccentBlue}"
                    BorderBrush="{DynamicResource AccentBlue}"
                    DockPanel.Dock="Right"
                    ToolTip="Agregar un nuevo producto">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Plus" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="NUEVO PRODUCTO"/>
                </StackPanel>
            </Button>

            <!-- Filtros -->
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" Margin="0,0,20,0" VerticalAlignment="Center">
                <TextBlock Text="Categoria:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource TextMuted}"/>
                <ComboBox ItemsSource="{Binding FiltrosCategoria}" 
                          SelectedItem="{Binding FiltroCategoria}" 
                          Width="120" Margin="0,0,15,0"
                          Style="{StaticResource CustomFilledComboBox}"/>
                          
                <TextBlock Text="Stock:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource TextMuted}"/>
                <ComboBox ItemsSource="{Binding FiltrosStock}" 
                          SelectedItem="{Binding FiltroStock}" 
                          Width="120" 
                          Style="{StaticResource CustomFilledComboBox}"/>
            </StackPanel>
        </DockPanel>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="5">

                <ItemsControl ItemsSource="{Binding Productos}" 
                              ItemTemplate="{StaticResource ProductoCardTemplate}">

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                </ItemsControl>

            </ScrollViewer>
    </Grid>
</UserControl>
