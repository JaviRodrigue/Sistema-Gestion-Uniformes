<UserControl x:Class="VentasApp.Desktop.Views.Productos.ProductoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:VentasApp.Desktop.ViewModels.Productos"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:ProductoViewModel/>
    </UserControl.DataContext>

    <Grid Background="{StaticResource SecondaryPastel}">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">

            <ItemsControl ItemsSource="{Binding Productos}" 
                          ItemTemplate="{StaticResource ProductoCardTemplate}">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

            </ItemsControl>

        </ScrollViewer>
    </Grid>
</UserControl>